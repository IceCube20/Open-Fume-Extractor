#pragma once

// Copy to MasterPrivateConfig.h for a private developer build. The real file
// is gitignored and must never be committed or included in release sources.
#define OFE_DEVELOPER_MODE_ENABLE 1
#define OFE_DEVELOPER_PASSWORD_SHA256 "64 lowercase SHA-256 hex characters"
